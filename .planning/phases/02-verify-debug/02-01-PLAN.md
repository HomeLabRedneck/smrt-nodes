---
phase: 02-verify-debug
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json"
  - "Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json"
  - "Smrt Thread Manager/kuuk-SmartThreadManager.zip"
  - "Smrt GPU Manager/kuuk-SmartGPUManager.zip"
  - "D:/Program Files (x86)/Steam/steamapps/common/Upload Labs/mods/kuuk-SmartThreadManager.zip"
  - "D:/Program Files (x86)/Steam/steamapps/common/Upload Labs/mods/kuuk-SmartGPUManager.zip"
autonomous: false
requirements: [CODE-01, FUNC-01, FUNC-02]

must_haves:
  truths:
    - "Both manifests include '2.1.11' in compatible_game_version array"
    - "Both ZIPs are repackaged with updated manifests and installed to game_dir/mods/"
    - "Game launches and both mods initialize (SUCCESS in modloader.log)"
    - "Baseline distribution test: downstream windows receive redistributed resource from STM/SGM"
  artifacts:
    - path: "Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json"
      provides: "STM manifest with 2.1.11 compatibility"
      contains: "2.1.11"
    - path: "Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json"
      provides: "SGM manifest with 2.1.11 compatibility"
      contains: "2.1.11"
    - path: "D:/Program Files (x86)/Steam/steamapps/common/Upload Labs/mods/kuuk-SmartThreadManager.zip"
      provides: "Installed STM mod with updated manifest"
    - path: "D:/Program Files (x86)/Steam/steamapps/common/Upload Labs/mods/kuuk-SmartGPUManager.zip"
      provides: "Installed SGM mod with updated manifest"
  key_links:
    - from: "manifest.json compatible_game_version"
      to: "GodotModLoader version check"
      via: "JSON array includes '2.1.11'"
      pattern: "2\\.1\\.11"
    - from: "Installed ZIPs in mods/"
      to: "modloader.log SUCCESS"
      via: "GodotModLoader scan on launch"
      pattern: "SUCCESS.*kuuk"
---

<objective>
Update both mod manifests to include game version 2.1.11 (the actual running version per GDRETools decompile), repackage and reinstall both mods, then verify baseline distribution behavior in-game.

Purpose: Research confirmed the game is 2.1.11 (not 2.1.10). Manifests currently only list ["2.1.8", "2.1.10"]. Adding "2.1.11" ensures the mod loader has no version mismatch issues. Then the critical question: does distribution actually work now that Phase 1 fixed the packaging? Research assumption A1 says "most likely yes" -- this plan tests that.

Output: Updated manifests, repackaged ZIPs, installed mods, and a human-verified baseline test result.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-verify-debug/02-RESEARCH.md
@.planning/phases/01-package-install/01-02-SUMMARY.md
@.planning/phases/01-package-install/01-03-SUMMARY.md

Key facts from research:
- Game version is 2.1.11 (from decompiled project.godot config/version)
- Both manifests currently have: "compatible_game_version": ["2.1.8", "2.1.10"]
- No API property renames exist -- all mod properties confirmed present in 2.1.11
- The mod loader logged SUCCESS for both mods at 2.1.10, but adding 2.1.11 is the correct fix per CODE-01
- Phase 1 Plan 02 established the packaging workflow: PowerShell Compress-Archive + Copy-Item to game_dir/mods/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update manifests for 2.1.11, repackage, and install both mods</name>
  <files>
    Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json
    Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json
    Smrt Thread Manager/kuuk-SmartThreadManager.zip
    Smrt GPU Manager/kuuk-SmartGPUManager.zip
    D:/Program Files (x86)/Steam/steamapps/common/Upload Labs/mods/kuuk-SmartThreadManager.zip
    D:/Program Files (x86)/Steam/steamapps/common/Upload Labs/mods/kuuk-SmartGPUManager.zip
  </files>
  <read_first>
    Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json
    Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json
    .planning/phases/01-package-install/01-02-SUMMARY.md
  </read_first>
  <action>
Per D-06 (fix both mods simultaneously) and CODE-01 (fix API/version mismatches):

1. Update STM manifest.json: change `"compatible_game_version"` from `["2.1.8", "2.1.10"]` to `["2.1.8", "2.1.10", "2.1.11"]`. Keep all other fields unchanged.

2. Update SGM manifest.json: same change -- `"compatible_game_version"` from `["2.1.8", "2.1.10"]` to `["2.1.8", "2.1.10", "2.1.11"]`. Keep all other fields unchanged.

3. Repackage both mods as ZIPs using the same method from Phase 1 Plan 02:
   - STM: From `Smrt Thread Manager/` directory, compress `mods-unpacked/` into `kuuk-SmartThreadManager.zip` (overwrite existing). ZIP root must be `mods-unpacked/kuuk-SmartThreadManager/...`.
   - SGM: From `Smrt GPU Manager/` directory, compress `mods-unpacked/` into `kuuk-SmartGPUManager.zip` (overwrite existing). ZIP root must be `mods-unpacked/kuuk-SmartGPUManager/...`.

4. Copy both ZIPs to `D:/Program Files (x86)/Steam/steamapps/common/Upload Labs/mods/` (overwrite existing).

5. Verify the installed ZIPs contain the updated manifest by extracting and checking the `compatible_game_version` field includes "2.1.11".
  </action>
  <verify>
    <automated>node -e "const m1=require('./Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json'); const m2=require('./Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json'); const ok1=m1.extra.godot.compatible_game_version.includes('2.1.11'); const ok2=m2.extra.godot.compatible_game_version.includes('2.1.11'); console.log('STM:', ok1, 'SGM:', ok2); if(!ok1||!ok2) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - STM manifest.json contains exact string `"2.1.11"` inside the compatible_game_version array
    - SGM manifest.json contains exact string `"2.1.11"` inside the compatible_game_version array
    - Both manifests still parse as valid JSON
    - kuuk-SmartThreadManager.zip exists in game_dir/mods/ with size > 1000 bytes
    - kuuk-SmartGPUManager.zip exists in game_dir/mods/ with size > 1000 bytes
    - ZIP-internal manifest.json contains "2.1.11" in compatible_game_version
  </acceptance_criteria>
  <done>Both manifests updated with 2.1.11, both ZIPs repackaged and installed to game mods directory, ZIP contents verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Baseline distribution test -- does smart allocation work?</name>
  <files>n/a</files>
  <action>
Human verifies in-game distribution behavior after manifest update and reinstall.

1. Close Upload Labs if running.
2. Launch Upload Labs.
3. Test SmartThreadManager (STM): Place a Smart Thread Manager window. Connect an upstream clock_speed source to its input. Connect two or more downstream windows (e.g., CPU windows) to its output. Wait a few seconds for ticks to process. Observe: Do the downstream windows receive different/proportional clock_speed values? (They should NOT all receive the full upstream amount -- that would be passthrough, not smart distribution.) Observe: Does the usage bar in the STM window show a non-zero percentage?
4. Test SmartGPUManager (SGM): Place a Smart GPU Manager window. Connect an upstream gpu_speed source to its input. Connect two or more downstream windows to its output. Wait a few seconds. Same checks as STM -- proportional distribution, non-zero usage bar.
5. Report results: "distribution works" (downstream windows get proportional amounts, usage bar moves) or "still broken" with description of symptom.
  </action>
  <verify>Human reports pass/fail for baseline distribution in both STM and SGM</verify>
  <acceptance_criteria>
    - Human provides clear pass/fail result for STM baseline distribution
    - Human provides clear pass/fail result for SGM baseline distribution
    - If pass: downstream windows receive proportional amounts (not full passthrough)
    - If pass: usage bar shows non-zero percentage in both mod windows
  </acceptance_criteria>
  <done>Baseline distribution test completed with clear pass/fail result for both STM and SGM. Result determines whether Plan 02 (diagnostics) is needed or can be skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mod manifest -> ModLoader | Mod loader reads manifest JSON; malformed JSON could crash loader |
| Mod scripts -> Game API | Mod calls game engine classes; mismatch causes silent failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | manifest.json | accept | Low risk: local-only mod, no distribution; user controls all files |
| T-02-02 | D (Denial of Service) | Malformed manifest JSON | mitigate | Verify JSON parses correctly after edit (node -e require()) |
</threat_model>

<verification>
- Both manifests contain "2.1.11" in compatible_game_version
- Both ZIPs are repackaged and installed
- Human confirms baseline distribution behavior (works or broken)
</verification>

<success_criteria>
- CODE-01 partially addressed: game version 2.1.11 identified and manifests updated
- Baseline distribution test completed with clear pass/fail result
- If PASS: FUNC-01 and FUNC-02 baseline confirmed (full mode testing in Plan 03)
- If FAIL: symptom documented for Plan 02 diagnostic instrumentation
</success_criteria>

<output>
After completion, create `.planning/phases/02-verify-debug/02-01-SUMMARY.md`

CRITICAL: Record the human checkpoint result verbatim. If "distribution works", Plan 02 becomes skippable and Plan 03 proceeds directly. If "still broken", Plan 02 is required before Plan 03.
</output>
