---
phase: 01-package-install
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json"
  - "Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json"
autonomous: true
requirements:
  - COMP-01
  - CODE-02
must_haves:
  truths:
    - "STM manifest version_number is exactly \"2.1.10\""
    - "SGM manifest version_number is exactly \"2.1.10\""
    - "STM manifest compatible_game_version is exactly [\"2.1.8\", \"2.1.10\"]"
    - "SGM manifest compatible_game_version is exactly [\"2.1.8\", \"2.1.10\"]"
    - "Both manifests still parse as valid JSON"
    - "Both smart_resource_container.gd files still begin with `extends ResourceContainer` (CODE-02 verification)"
  artifacts:
    - path: "Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json"
      provides: "STM manifest with 2.1.10 compatibility"
      contains: "\"version_number\": \"2.1.10\""
    - path: "Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json"
      provides: "SGM manifest with 2.1.10 compatibility"
      contains: "\"version_number\": \"2.1.10\""
  key_links:
    - from: "STM manifest.json compatible_game_version array"
      to: "GodotModLoader version check at mod load time"
      via: "JSON array string match"
      pattern: "\"2.1.10\""
    - from: "SGM manifest.json compatible_game_version array"
      to: "GodotModLoader version check at mod load time"
      via: "JSON array string match"
      pattern: "\"2.1.10\""
---

<objective>
Update both mod manifests so GodotModLoader recognizes them as compatible with Upload Labs 2.1.10.

Purpose: Without 2.1.10 in compatible_game_version, the loader (even if it doesn't enforce strictly) is mismatched against the running game. Both manifests currently advertise as 2.1.5 with legacy compatibility list — they need to be re-stamped per D-02, D-04, D-05. CODE-02 is verified here as a free check since both source trees are already in front of us.

Output: Two updated manifest.json files in their source locations, ready to be packaged in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-package-install/01-CONTEXT.md
@.planning/phases/01-package-install/01-RESEARCH.md
@.planning/phases/01-package-install/01-PATTERNS.md

<interfaces>
<!-- Manifest JSON schema. Both mods use this identical structure. -->
<!-- Only three keys are touched: version_number, compatible_game_version. -->
<!-- compatible_mod_loader_version is NOT changed (already correct per D-03 research resolution). -->

Current manifest.json shape (both mods):
```json
{
    "dependencies": [],
    "description": "Smart distributor for {CPU|GPU} speed.",
    "extra": {
        "godot": {
            "authors": ["kuuk"],
            "compatible_game_version": ["2.0.0", "2.0.17", "2.0.19", "2.0.20", "2.0.21", "2.1.8"],
            "compatible_mod_loader_version": ["7.0.0", "7.0.1"],
            "config_schema": {},
            "description_rich": "",
            "image": null,
            "incompatibilities": [],
            "load_before": [],
            "optional_dependencies": [],
            "tags": ["gameplay", "qol"]
        }
    },
    "name": "{SmartThreadManager|SmartGPUManager}",
    "namespace": "kuuk",
    "version_number": "2.1.5",
    "website_url": "https://github.com/Omisse/ul-stmmod"
}
```

Target manifest.json shape (both mods, after edits):
```json
{
    ... (unchanged) ...
    "extra": {
        "godot": {
            ... (unchanged) ...
            "compatible_game_version": ["2.1.8", "2.1.10"],
            "compatible_mod_loader_version": ["7.0.0", "7.0.1"],
            ... (unchanged) ...
        }
    },
    ... (unchanged) ...
    "version_number": "2.1.10",
    ... (unchanged) ...
}
```

CODE-02 verification target:
```gdscript
# First non-empty line of smart_resource_container.gd (both mods):
extends ResourceContainer
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SmartThreadManager manifest to 2.1.10 (D-02, D-04, D-05)</name>
  <files>Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json</files>
  <read_first>
    - Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json (current state — must see exact whitespace and formatting before editing)
    - .planning/phases/01-package-install/01-PATTERNS.md (lines 26-91 — exact target JSON shape for STM)
  </read_first>
  <action>
Apply three exact edits to `Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json`. Do NOT reformat anything else. Preserve the file's existing tab indentation.

Edit 1 (D-04) — change version_number:
  FROM: `"version_number": "2.1.5",`
  TO:   `"version_number": "2.1.10",`

Edit 2 (D-02 + D-05) — replace the compatible_game_version array entirely. The current block spans 8 lines:
```
				"compatible_game_version": [
					"2.0.0",
					"2.0.17",
					"2.0.19",
					"2.0.20",
					"2.0.21",
					"2.1.8"
				],
```
Replace those 8 lines with:
```
				"compatible_game_version": [
					"2.1.8",
					"2.1.10"
				],
```

Edit 3 (D-03 verification only — NO file change):
  Confirm `"compatible_mod_loader_version": ["7.0.0", "7.0.1"]` is present and unchanged. Per RESEARCH.md, 7.0.1 is the latest GodotModLoader release; nothing to add.

Do not touch any other field (description, namespace, name, website_url, tags, etc).
  </action>
  <verify>
    <automated>node -e "const m=require('./Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json'); const v=m.version_number==='2.1.10'; const cgv=JSON.stringify(m.extra.godot.compatible_game_version)===JSON.stringify(['2.1.8','2.1.10']); const cmlv=JSON.stringify(m.extra.godot.compatible_mod_loader_version)===JSON.stringify(['7.0.0','7.0.1']); if(!v||!cgv||!cmlv){console.error('FAIL',{v,cgv,cmlv,manifest:m});process.exit(1);} console.log('OK STM manifest');"</automated>
  </verify>
  <acceptance_criteria>
    - Command `node -e "const m=require('./Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json'); console.log(m.version_number)"` prints exactly `2.1.10`
    - Command `node -e "const m=require('./Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json'); console.log(JSON.stringify(m.extra.godot.compatible_game_version))"` prints exactly `["2.1.8","2.1.10"]`
    - Command `node -e "const m=require('./Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json'); console.log(JSON.stringify(m.extra.godot.compatible_mod_loader_version))"` prints exactly `["7.0.0","7.0.1"]`
    - Grep: `grep -c "2.0.0" "Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json"` returns `0` (legacy version removed)
    - Grep: `grep -c "2.0.17" "Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json"` returns `0`
    - Grep: `grep -c "\"namespace\": \"kuuk\"" "Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json"` returns `1` (namespace untouched)
    - Grep: `grep -c "\"name\": \"SmartThreadManager\"" "Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/manifest.json"` returns `1` (name untouched)
    - File parses as valid JSON (the node -e command above will throw if not)
  </acceptance_criteria>
  <done>
    STM manifest contains `"version_number": "2.1.10"`, `compatible_game_version` is exactly `["2.1.8", "2.1.10"]`, `compatible_mod_loader_version` is unchanged at `["7.0.0", "7.0.1"]`, and the file remains valid JSON. No other fields modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SmartGPUManager manifest to 2.1.10 + verify CODE-02 in both mods</name>
  <files>Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json</files>
  <read_first>
    - Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json (current state — note: this file uses 4-space indentation, NOT tabs like the STM manifest; preserve it)
    - .planning/phases/01-package-install/01-PATTERNS.md (lines 99-159 — exact target JSON shape for SGM)
    - Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/scripts/smart_resource_container.gd (CODE-02 verification — confirm first non-empty line)
    - Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/scripts/smart_resource_container.gd (CODE-02 verification — confirm first non-empty line)
  </read_first>
  <action>
Step A — Apply three exact edits to `Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json`. Preserve the file's existing 4-space indentation (this file uses spaces, not tabs).

Edit 1 (D-04) — change version_number:
  FROM: `"version_number": "2.1.5",`
  TO:   `"version_number": "2.1.10",`

Edit 2 (D-02 + D-05) — replace the compatible_game_version array entirely. The current block spans 8 lines:
```
            "compatible_game_version": [
                "2.0.0",
                "2.0.17",
                "2.0.19",
                "2.0.20",
                "2.0.21",
                "2.1.8"
            ],
```
Replace those 8 lines with:
```
            "compatible_game_version": [
                "2.1.8",
                "2.1.10"
            ],
```

Edit 3 (D-03 verification only — NO file change):
  Confirm `"compatible_mod_loader_version": ["7.0.0", "7.0.1"]` is present and unchanged.

Do not touch any other field. Do not change `"name": "SmartGPUManager"`, `"namespace": "kuuk"`, `"description": "Smart distributor for GPU speed."`, etc.

Step B — Verify CODE-02 in both mod source trees by reading the first non-empty line of each `smart_resource_container.gd`. Each must equal `extends ResourceContainer`. If either does not match, REPORT IT — do NOT modify the .gd file in this plan (CODE-02 fix would be a separate scope).

Files to inspect:
  - `Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/scripts/smart_resource_container.gd`
  - `Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/scripts/smart_resource_container.gd`

Note the location of `smart_resource_container.gd` may be in `scripts/` or a different subdir — use `find` or `ls` to locate if not at the path above. Both mods should have the file under the `scripts/` subdirectory based on the source listing in STATE.md and the file tree confirmed in PATTERNS.md.
  </action>
  <verify>
    <automated>node -e "const m=require('./Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json'); const v=m.version_number==='2.1.10'; const cgv=JSON.stringify(m.extra.godot.compatible_game_version)===JSON.stringify(['2.1.8','2.1.10']); const cmlv=JSON.stringify(m.extra.godot.compatible_mod_loader_version)===JSON.stringify(['7.0.0','7.0.1']); if(!v||!cgv||!cmlv){console.error('FAIL',{v,cgv,cmlv,manifest:m});process.exit(1);} console.log('OK SGM manifest');"</automated>
  </verify>
  <acceptance_criteria>
    - Command `node -e "const m=require('./Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json'); console.log(m.version_number)"` prints exactly `2.1.10`
    - Command `node -e "const m=require('./Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json'); console.log(JSON.stringify(m.extra.godot.compatible_game_version))"` prints exactly `["2.1.8","2.1.10"]`
    - Command `node -e "const m=require('./Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json'); console.log(JSON.stringify(m.extra.godot.compatible_mod_loader_version))"` prints exactly `["7.0.0","7.0.1"]`
    - Grep: `grep -c "2.0.0" "Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json"` returns `0` (legacy version removed)
    - Grep: `grep -c "\"name\": \"SmartGPUManager\"" "Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json"` returns `1` (name untouched)
    - Grep: `grep -c "\"description\": \"Smart distributor for GPU speed.\"" "Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/manifest.json"` returns `1` (description untouched)
    - CODE-02 STM check: first non-blank, non-comment line of `Smrt Thread Manager/mods-unpacked/kuuk-SmartThreadManager/scripts/smart_resource_container.gd` equals exactly `extends ResourceContainer`
    - CODE-02 SGM check: first non-blank, non-comment line of `Smrt GPU Manager/mods-unpacked/kuuk-SmartGPUManager/scripts/smart_resource_container.gd` equals exactly `extends ResourceContainer`
    - SUMMARY explicitly records both CODE-02 file paths and confirms both extend `ResourceContainer`
  </acceptance_criteria>
  <done>
    SGM manifest contains `"version_number": "2.1.10"` and `compatible_game_version: ["2.1.8", "2.1.10"]`, parses as JSON, no other fields touched. CODE-02 verified in both mods (both `smart_resource_container.gd` files begin with `extends ResourceContainer`) and recorded in SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→manifest.json | Manual JSON edit; risk of malformed JSON breaking loader |
| manifest.json→GodotModLoader | Loader parses manifest at startup; bad JSON = silent skip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | manifest.json (hand-edit risk) | mitigate | Verify with `node -e "require('./manifest.json')"` after edit; acceptance_criteria includes JSON parse check |
| T-01-02 | Denial of Service | GodotModLoader (malformed manifest crashes load) | mitigate | Both manifests must parse as valid JSON before Plan 02 packages them; verification in acceptance_criteria |
| T-01-03 | Information Disclosure | manifest.json (no PII, public mod metadata) | accept | Manifests are public mod info; no secrets present |
| T-01-04 | Spoofing | namespace/name fields (could mislead loader to load wrong mod) | mitigate | Acceptance criteria pins exact `"name"` and `"namespace"` values to prevent accidental rename |
</threat_model>

<verification>
After both tasks complete, the SUMMARY must show:
1. Both manifests parse as valid JSON (node -e require check)
2. STM manifest: version_number=2.1.10, compatible_game_version=["2.1.8","2.1.10"]
3. SGM manifest: version_number=2.1.10, compatible_game_version=["2.1.8","2.1.10"]
4. CODE-02 confirmed at source level in both `smart_resource_container.gd` files
5. No other manifest fields were modified (namespace, name, description, etc. all intact)
</verification>

<success_criteria>
- COMP-01 satisfied: both manifests list "2.1.10" in compatible_game_version
- CODE-02 satisfied: both `smart_resource_container.gd` files verified to extend ResourceContainer
- D-02, D-04, D-05 implemented exactly per spec
- D-03 confirmed no-op (compatible_mod_loader_version already at 7.0.1)
- Both manifests are valid JSON (parseable with `node -e "require(...)"`)
- Plan 02 has correct, version-stamped manifests to package into ZIPs
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-install/01-01-SUMMARY.md` documenting:
- Diffs applied to both manifests (before/after for the two changed fields each)
- CODE-02 verification result for both `smart_resource_container.gd` files (path + first non-blank line)
- Confirmation that compatible_mod_loader_version was unchanged (D-03 no-op rationale)
- Any deviations from the planned edits (should be none)
</output>
